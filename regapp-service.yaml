apiVersion: v1
kind: Service
metadata:
  name: regapp-service
  namespace: congthanh 
  labels:
    app: regapp
spec:
  selector:
    app: regapp
  ports:
  - port: 80          
    targetPort: 8080   
    protocol: TCP
    name: http
  type: LoadBalancer